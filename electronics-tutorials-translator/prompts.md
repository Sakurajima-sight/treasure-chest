html2markdown

```
请将上述 HTML 内容严格按原有顺序转换为 Markdown 格式，不要出现任何HTML的样式，句子保持原意，同时注意句子的连续性，不做翻译、改写、增删或润色。所有公式使用 LaTeX 数学格式。遇到 <img> 标签时，若图片链接以 assets 开头，请使用 Markdown 图片语法并保留本地图片路径，不加链接；若为其它相对路径或绝对路径（如以 /wp-content/uploads/ 等开头），请在路径前自动补全 https://www.electronics-tutorials.ws/ 前缀后，使用 Markdown 图片语法保留图片链接；若为网络图片（如以 http 或 https 开头），直接使用原链接。遇到 prev_next 类标签时，从该标签起后的内容全部忽略（该规则优先级最高）。输出仅包含纯 Markdown 正文，不含任何额外说明。 
```

markdown2bilingual

```
所有标题和子标题需要进行对照翻译，翻译的中文标题跟在英文标题后面，也就是说英文标题和翻译的中文标题是同一行，英文标题和翻译的中文标题之间除了空格不要添加任何其他内容。请将每段可能包含的多句话拆分为单独的句子逐句翻译，要注意按顺序翻译，并确保英文和中文的对照。翻译后的内容格式应与原文的Markdown格式一致，latex数学公式要适当优化，注意下标，每个英文句子后紧跟对应的中文翻译，但英文句子和他的中文句子翻译需要换行，并确保每个英文句子之间隔开一行。若有图片，图片也要按顺序存放，只需要保留唯一一张图片，不添加其他信息。
```

